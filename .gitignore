# Dependencies
frontend/node_modules/

# Next.js build output
frontend/.next/
frontend/out/

# Environment variables — never commit these
.env
.env.local
.env*.local
frontend/.env.local
backend/.env

# Go build artifacts
backend/sonic
backend/*.exe

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Local design reference material (source PDFs) — keep local, never commit
design-assets/
# Company-profile PDF — large content source, kept local (not served/linked)
frontend/public/SONIC--_.pdf
